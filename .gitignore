# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
build
out
.next
.turbo
*.tsbuildinfo

# env / secrets — NEVER commit
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
*.pem
*.key
*.keystore
secrets/
.secret
.secrets

# AXL node identities — Ed25519 private keys live in node configs
axl-node-config.json
axl-node-config.*.json
**/axl-data/
**/.axl/
*.axl-key

# 0G / wallet keys
wallet.json
deployer.json
.deployer
.broker-secret

# Foundry
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/lib/
!contracts/lib/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.python-version
*.egg-info/

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.swp
*.swo

# logs
*.log
npm-debug.log*
yarn-debug.log*
bun-debug.log*

# coverage
coverage/
*.lcov
.nyc_output/

# misc local
.cache/
tmp/
.tmp/
.local/

# Obsidian vault paths or sample data — never commit user content
sample-vault/
*.vault-snapshot.json
# Exception: the curated demo vault that ships with the repo.
!scripts/demo/sample-vault/

# Local Claude Code agent state
.claude/*
# Team-shared gstack enforcement (committed for all contributors)
!.claude/hooks/
!.claude/settings.json
.env.*.bak
.env.testnet.bak
.gstack/
